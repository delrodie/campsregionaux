{{ form_start(form)}}
<div class="row form-group">
    <div class="col-sm-8">{{ form_row(form.matricule) }}</div>
    <div class="col-sm-4">
        {{form_widget(form)}}
        <button class="btn btn-primary">Rechercher</button>
    </div>
</div>
{{form_end(form)}}