{{ form_start(form,{'attr':{'class':'contact_form mt-md-4'}})}}
<div class="row">
    <div class="col-md-6 mt-3">{{ form_row(form.nom) }}</div>
    <div class="col-md-6 mt-3">{{ form_row(form.prenoms) }}</div>
    <div class="col-md-6 mt-3">{{ form_row(form.datenaiss) }}</div>
    <div class="col-md-6 mt-3">{{ form_row(form.lieunaiss) }}</div>
</div>
<div class="row">
    <div class="col-md-12 mt-3">
        <div class="d-flex justify-content-end">
            <button class="mt-2 btn theme-btn contact-form-btn border-0" value="Send">Recherche</button>
        </div>
    </div>
</div>
<div class="row d-md-none">
    <div class="col-sm-6 mt-5 text-left">
        <a href="{{ path('app_search_civil', {'region': region.slug}) }}">J'ai oublié mon matricule</a>
    </div>
    <div class="col-sm-6 mt-3 text-left">
        <a href="https://adhesion.scoutascci.org/adhesion/inscription/nouvelle/">Je n'ai pas encore de matricule</a>
    </div>
</div>
<div class="row ">
    <div class="col-md-6 mt-5 text-right d-none d-md-block">
        <a href="{{ path('app_search_civil', {'region': region.slug}) }}">J'ai oublié mon matricule</a>
    </div>
    <div class="col-md-6 mt-5 text-left d-none d-md-block">
        <a href="https://adhesion.scoutascci.org/adhesion/inscription/nouvelle/" target="_blank">Je n'ai pas encore de matricule</a>
    </div>
</div>

{{form_end(form)}}
